# flow · 项目总结

> 一句话：面向「任务 → 期次 → 人员」模型的**线性顺序流转工作流平台**。管理员设计流程模板（有序节点链 + 各节点表单），创建主任务并按周期生成期次，每个期次为每名任务人员创建一条独立成员任务，从开始节点按链逐节点流转，直到结束节点完成。
>
> 区别于并行表单收集：不是所有人同时填一张表，而是一步一步往前走，每步处理人不同、每步字段不同，提交时由处理人指定下一节点处理人。

---

## 一、技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue 2.6 + Element UI 2.13 + Vuex + Vue Router + Axios（vue-admin-template 改造） |
| 后端 | Spring Boot 3.4 + Java 21，RESTful 接口 |
| ORM | MyBatis-Plus 3.5.5（实体 + Mapper + XML 联表） |
| 认证 | JWT（jjwt），登录签发 `Authorization: Bearer` |
| 数据库 | MySQL 8 + InnoDB + utf8mb4，库名 `flow`，17 张表 |
| 构建 | Maven 3.9 / vue-cli 4 |

---

## 二、核心业务模型

```
流程模板 flow_template（节点链 flow_template_node + 字段 flow_template_field，版本化 flow_template_version）
   └─ 主任务 flow_dispatch（任务级字段 + 周期 flow_dispatch_config + 人员 flow_task_member）
        └─ 期次 flow_task_dispatch（快照节点 flow_task_dispatch_node，锁定期次下发时的模板版本）
             └─ 成员任务 flow_task（每人一条独立流程，含流程指针/进度）
                  └─ 节点记录 flow_task_node（含表单留痕 flow_form_record → flow_form_data）
```

关键机制：

- **节点类型**：开始(1) / 中间(2) / 结束(3)；**多处理人**节点「任一完成即可」，其余并行分支自动消解。
- **退回重做**：可退回到已到达过的前置节点，目标节点表单回填最近一次真实提交的数据，可修改重交。
- **进度口径**：成员卡片展示 `已完成节点数 / 总节点数`（第一个节点未完成即 `0/5`）。
- **样例机制**：`is_sample=1` 数据公共可见、可复制复用；「设为样例」仅超管。
- **部门口径**：以 `dept_admin` 登记的部门为准（登录人 `yyyt_id` 反查 `dept_admin`），`aut_user.dept_id` 不作为权限依据。

---

## 三、功能模块

| 模块 | 说明 | 前端（`flow-front/src/views`） | 后端（`com.company.flow`） |
|---|---|---|---|
| 操作指引 | 首页使用说明 | `dashboard` | — |
| 任务处理 | 处理人待办：填当前节点表单 → 指定下一处理人 → 通过流转；支持退回 | `taskprocess` | `flowtask`（`FlowTaskService.submit`） |
| 任务管理 | 创建/复制主任务、生成/复制期次、期次人员查看与催办 | `flowdispatch` | `flowtask`（`FlowDispatchService`） |
| 模板管理 | 流程模板：节点链 + 节点字段、启停、复制、版本化 | `flowtemplate` | `flowtemplate` |
| 表单设计器 | 节点链 + 字段三栏可视化设计（隐藏路由） | `formdesigner` | `flowtemplate` |
| 下发配置模板 | 周期/触发规则配置库，新建任务一键拉取 | `flowdispatch/ConfigTemplate*` | `flowtask`（ConfigTemplate） |
| 数据展示 | 任务数据报表与明细 | `dataview` | `flowdata` |
| 系统管理员 | dept_admin 部门管理员与部门维护 | `deptadmin` | `base/deptAdmin` |
| 用户 | aut_user 全量用户表与登录 | `login` / `sysuser` | `base/autuser` |

---

## 四、目录结构

### 后端 `flow-service/src/main/java/com/company/flow/`

```
base/            # FlowApplication 启动类 + 基础设施
  ├─ attach/     # 附件（冻结模块）
  ├─ autuser/    # 用户 / 登录（controller/entity/mapper/service/vo）
  ├─ config/     # AdminProperties 超管白名单、AuthInterceptor、FlowIdGenerator、
  │              #   MybatisPlusConfig、WebConfig
  ├─ context/    # UserContext（仅 controller 层可用）
  ├─ deptAdmin/  # 部门管理员（冻结模块，反查部门口径）
  ├─ exception/  # GlobalExceptionHandler
  ├─ job/        # 定时任务：PeriodAutoDispatchJob / RemindNodeJob /
  │              #   SyncTemplateFieldsJob / UrgeJob（@EnableScheduling）
  ├─ result/     # Result / PageResult 统一返回
  ├─ robot/      # 机器人通知
  └─ util/       # BaseMapper / DateUtil / SecurityUtils / UUIDUtil（冻结模块）
flowtemplate/    # 模板 / 节点 / 字段 / 版本
flowtask/        # 任务 / 期次 / 成员 / 流转（核心：FlowTaskService、FlowDispatchService）
flowdata/        # 数据查看与报表
```

### 前端 `flow-front/src/`

```
views/
  ├─ dashboard/     # 操作指引
  ├─ taskprocess/   # 任务处理（我的待办 + 流程详情）
  ├─ flowdispatch/  # 任务管理（列表/编辑/期次人员/人员流程/按人查看/配置模板）
  ├─ flowtemplate/  # 模板管理
  ├─ formdesigner/  # 表单设计器
  ├─ dataview/      # 数据展示
  ├─ deptadmin/     # 系统管理员
  └─ login/         # 登录
components/         # 全局组件：AttachField / FlowChain / UserPicker / BaseModal 等
service/            # 接口封装：base/（Attach/DeptAdmin/User）+ sys/（Data/Dispatch/Task/Template）
router/             # 路由（不做路由级权限控制，数据由后端按登录用户区分）
store/              # Vuex
```

### 文档与辅助目录（根目录）

| 路径 | 说明 |
|---|---|
| `README.md` | 总览 + 文档导航 + 开发约定（最新） |
| `docs/项目开发文档.md` | 全量开发文档（**部分为早期版本，包名/字段以代码为准**） |
| `readme.txt` | 早期业务方案说明 |
| `.trae/documents/` | 历史改造/实施计划（5 篇） |
| `.workbuddy/memory/` | 按日开发会话日志 |
| `flow_schema.sql` | 数据库初始化建表脚本（17 张表） |
| `flow_template5_import.sql` | 测试模板数据导入脚本 |
| `stitch_flow_task_collection_system/` | 独立 UI 设计样例（Axiom Workflow 设计系统，不参与主工程构建） |
| `outputs/` | 页面截图等产物 |

---

## 五、数据库表（17 张）

| 表名 | 用途 |
|---|---|
| `sys_user` | 系统用户（账号 / 员工号 / 姓名 / 部门 / 密码） |
| `flow_template` | 流程模板主表 |
| `flow_template_node` | 模板节点链【核心】 |
| `flow_template_field` | 节点表单字段（JSON 存枚举选项） |
| `flow_template_version` | 模板版本快照（config_snapshot JSON） |
| `flow_dispatch` | 主任务（任务级字段 + 周期 + 人员） |
| `flow_dispatch_config` | 每任务一份的下发周期配置 |
| `flow_dispatch_config_template` | 下发配置模板库 |
| `flow_task_member` | 任务人员配置 |
| `flow_task_dispatch` | 期次 |
| `flow_task_dispatch_node` | 期次下发时的节点快照 |
| `flow_task` | 成员任务（每人一条独立流程） |
| `flow_task_node` | 任务流转节点记录【核心】 |
| `flow_task_log` | 流转 / 催办日志 |
| `flow_form_record` | 表单填报主记录 |
| `flow_form_data` | 表单字段填报值 |
| `attach` | 附件（工具表） |

---

## 六、本地启动

### 环境要求

JDK 21 · Maven 3.9 · Node 16+ · MySQL 8

### 1. 初始化数据库

```bash
mysql -uroot -p < flow_schema.sql              # 建库 flow + 全部表
mysql -uroot -p flow < flow_template5_import.sql  # 导入测试模板（可选）
```

数据源见 `flow-service/src/main/resources/application.yml`：`jdbc:mysql://localhost:3306/flow`（root/12345）。

### 2. 启动后端（端口 9000）

```bash
cd flow-service
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=9000
```

### 3. 启动前端（dev 默认 9528；本仓库调试常以 9532 运行）

```bash
cd flow-front
npm install
npm run dev -- --port 9532
```

前端访问 `http://localhost:9532`；接口经 `vue.config.js` 将 `/api` 代理到 `http://localhost:9000`。

### 4. 测试账号（统一密码 `123456`）

| 账号 | 姓名 | 说明 |
|---|---|---|
| emp0001 | 张三 | 超管（yml `system.admin.userIds: emp0001`），技术部部门管理员 |
| emp0002 | 李四 | 市场部部门管理员 |
| emp0003 | 王五 | 技术部部门管理员 |

---

## 七、关键开发约定

1. **权限判断一律走后端**：前端不依据 `userInfo` 做权限/身份分支；需隐藏按钮时由后端列表返回 `isCreator`/`isMe`/`canSubmit` 等标识。
2. **用户标识**：统一用用户号 `yyyt_id` + 姓名 `user_name`，一切匹配/通知按 `yyyt_id`，禁止按姓名匹配（重名风险）。
3. **部门口径**：用 `dept_admin` 反查，勿用 `aut_user.dept_id`。
4. **主键**：32 位（表名缩写 + 时间戳 + 随机数），非自增（`FlowIdGenerator`）。
5. **后端 `UserContext` 仅限 controller 层**：service 层禁止 import。
6. **冻结模块**：`attach` / `process` / `deptAdmin` / `util` 不允许改动，如需变更须先书面说明经同意。
7. **前端每改完文件跑一次 lint**：dev 开启 `lintOnSave`，eslint error 会阻塞热更新导致页面刷不出来。
8. **任务进度**：成员卡片进度来自 `nodeSteps` 的 done 计数，勿直接展示 `flow_task.finished_node_count`（退回后虚高）。

---

*维护提示：大版本改动后请同步更新本文档与 `README.md`；按日开发日志持续追加到 `.workbuddy/memory/`。*
