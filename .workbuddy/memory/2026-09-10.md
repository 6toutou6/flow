# 2026-09-10 工作日志

## 期次任务关联：弹窗 → 独立页面

皇上要求把「期次任务关联」弹窗改成页面。

- 新增页面 `views/flowdispatch/TaskLink.vue`（路由 `/flow-dispatch/task-link`，name `FlowDispatchTaskLink`，hidden，activeMenu /flow-dispatch/index）：页头(返回+面包屑)、来源期次 tip-bar、已关联表格(任务名称/状态/流程进度/操作) + 行展开 FlowChain(横/竖) + rel-extra(关联说明/时间/解除)；新增关联走弹窗组件。路由参数 query: dispatchId(任务配置)、taskName、periodId(期次)、periodName。
- 新增组件 `components/AddTaskLinkModal.vue`（仅"新增关联"弹窗）：props visible/sourceDispatchName/sourcePeriodId/sourcePeriodName/linkedTaskIds(排除已关联)，提交 createTaskLink。
- `views/flowdispatch/index.vue`：期次行「关联」按钮改 `goTaskLink(t,p)` 路由跳转；移除 RelateTaskModal 挂载/import/components 注册/relateVisible|relateTask|relatePeriod data/openRelate/onRelateCreated。
- 删除 `components/RelateTaskModal.vue`（已被 AddTaskLinkModal + TaskLink 页面替代）。
- 后端无改动。前端 eslint 0 error（dev server HMR 编译正常，仅 ConfigTemplateEdit 既有 html-indent warning）。
