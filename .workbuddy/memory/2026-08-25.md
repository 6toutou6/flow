# 2026-08-25 工作日志

## 09:13 启动前后端
- 隔夜服务已停止，重新启动：后端 9000（mvn spring-boot:run）、前端 9532（npm run dev），就绪并登录验证通过（emp0001/123456 → 张三 超管）；按服务保持准则保持在线 2 小时+

## 09:17 参考 gzfb 改路由参数与样式（去掉页面主体离顶距离）
- **路由参数**：flow 19 处 router.push 全部用 query（与 gzfb 一致），无 params 残留——参数已合规无需改
- **上边距修复**：layout/index.vue `.main-container` 写死 `padding-top: 56px` + AppMain `.fixed-header+.app-main` 也有 padding-top: 56px = **双重让位**约 112px 空隙
- **修改**：main-container padding-top: 56px → 0（fixed navbar 悬浮由 app-main 一处让位即可，避免重复）
- **验证**：navbar 56px（fixed）、app-main 顶部 0、page-content 顶部 56px（紧贴 navbar 下方），截图确认无多余空隙

## 09:21 返回按钮移到面包屑左侧
- **需求**：跳转界面后返回按钮放左边（路径/面包屑左边）
- **改动 5 个页面**：EditTask/PeriodFlow/PeriodUsers/ConfigTemplateEdit（返回按钮从 page-header 右侧移到左侧）+ PersonView（返回任务管理从 header-actions 拆出，刷新按钮保留右侧）
- **结构**：page-header > .header-left（btn-back + 面包屑/标题）+ .header-actions（右侧操作）；新增 .header-left 样式（flex align-items:flex-start gap:14px）
- **验证**：编辑任务页返回按钮在面包屑左侧 ✅，编译通过

## 09:26 调整左上角布局（返回按钮不再挤）
- **问题**：返回按钮带边框背景、垂直与面包屑错位、间距太小，"挤成一坨"
- **调整**：.header-left gap 14→20px + align-items center（垂直居中对齐）；.btn-back 去边框/去背景、padding 减小、hover 给 8% 主色淡底 → 变轻量文字按钮
- **验证**：截图确认左上角清爽，「← 返回」与面包屑/标题有清晰间距

## 09:28 按钮与路径同行、标题下行、两行左对齐
- **需求**：返回按钮和路径同一行、页面名称下一行、两行左对齐
- **结构调整**（5 页面）：header-left 纵向两行（flex-direction column）；`.hl-row1`（按钮+面包屑同一行，align-items center gap 14px）；标题 h3 独立下一行
- **踩坑**：python 批量替换时误删 header-left 的 </div> 闭合 → 模板不平衡 → 组件不渲染（vue-template-compiler 报 tag <div> has no matching end tag）；已补回闭合，5 个模板全部编译 OK
- **验证**：btnTop 80 vs bcTop 81（同行 ✅）；hTop 135（标题下一行 ✅）；btnLeft 264 = hLeft 264（两行左对齐 ✅）

## 09:39 清理任务提交数据（重新测试系统）
- **删除**（11 张表清零，先子后父）：flow_form_data(59) / flow_form_record(5) / flow_task_log(9) / flow_task_node(9) / flow_task_member(2) / flow_task_user(0) / flow_task_dispatch_node(10) / flow_task(4) / flow_task_dispatch(2) / flow_dispatch_config(1) / flow_dispatch(1)
- **保留**：模板（flow_template 1 + node 5 + field 62 + version 8）、下发配置模板（1）、aut_user(17)、dept_admin(4)、attach/flow_attachment（0）
- **验证**：任务管理列表 total 0 ✅；模板列表 total 1（整改-复杂模板）✅；配置模板 1 ✅

## 10:04 服务被关掉原因排查 + 重启
- **现象**：09:13 启动的前后端（EcLGxD/M69vhd）在 10:04 前已停止，后台任务记录 not found（任务被清）
- **根因**：run_in_background 启动的服务进程依附于 WorkBuddy 宿主会话；宿主会话期间被关闭/重启（或系统休眠/会话清理）→ 后台任务及子进程一并被终止。非代码问题、非"时间到自动断"
- **处理**：重新启动后端 9000（d0TYZh）+ 前端 9532（uVeeJ3），就绪并登录验证通过
- **注意**：保持 WorkBuddy 会话打开/电脑不休眠，服务可持续在线；若会话再次被清理需重新启动

## 10:10 修复两个下发问题
- **问题1：下发期次任务人收不到**——根因：生成期次时 flow_task.setStatus("启用")（沿用了 flow_dispatch 语义），但全系统 flow_task 状态是中文语义'进行中'/'已结束'/'已作废'，selectMyTodo 等 SQL 查 `t.status IN ('进行中','已结束')` 匹配不到 → 待办 total 0
  - 修复：FlowDispatchService 两处 ft.setStatus("启用")→"进行中"；存量 UPDATE flow_task SET status='进行中'；前端 PeriodUsers statusText/statusClass/筛选下拉改中文状态（'进行中'/'已完成'/'已作废'）
- **问题2：期次人员页员工名字旁工号显示不对**——根因：FlowTaskMapper.xml `NULL AS ownerEmpNo` 硬编码空 → 显示 '—'
  - 修复：ownerEmpNo 改为 u.yyyt_id（显示 emp0001 等）
- **验证**：emp0002 待办 total 2（两期次）；grouped 显示任务+期次+待办节点（问题自查）+5 节点链；期次成员张三 emp0001 技术部/进行中 ✅
- 附带确认：新建任务 500 是 curl 路径写错（应为 /flow-dispatch/save），系统正常

## 11:51 重启前后端
- 会话再次被清理导致服务停止，重新启动：后端 9000（zaevIf）、前端 9532（gYcUoV），就绪并登录验证通过
