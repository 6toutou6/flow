# 2026-09-09 工作日志

## FlowChain 会签提交展示改为「一行一人、点击查看」

皇上反馈：会签节点提交记录之前全部展开过长，要求「先一行一行展示人，然后可以查看提交表单数据」。

- 横/竖展开区：会签节点（formSubmissions>1）显示「提交记录（N 人，点击处理人查看表单）」；每人一行（序号+姓名+工号+提交时间+「查看表单/收起」+箭头），点击该行就地展开该人的任务基础信息+表单字段；同一时间只展开一人（accordion，`activeSubId`）。
- 单份（普通节点）默认直接展示该份内容（无折叠）。
- 新增 `data.activeSubId`、`methods.toggleSub/isSubViewed`；watch taskDetail 重置。
- 样式：`.sub-head.clickable` hover、`.sub-item.active`、`.sub-arrow` 旋转动画。
- 验证：前端 eslint 0 error。

## FlowChain 会签提交记录支持多行展开 + 一键展开/收回

皇上要求会签提交记录可同时展开多行，并提供一键全部展开/收回：

- `expandedSubIds`（数组）替代原单值 `activeSubId`：点击行 toggle 单个、互不影响；`isSubOpen(sub, total)` 判定展示（单份直接展示）。
- 标题行右侧新增「全部展开 / 全部收回」文本操作（`.sub-title-bar` + `.sub-tools`，仅会签多份时显示），`setAllSubs` 批量设置。
- watch taskDetail 重置 expandedSubIds。
- 验证：前端 eslint 0 error。

## 其他

- 昨天（09-08）修正的「退回会签节点语义」（reject 分支：目标为会签节点时不把退回者建入会签待办，只补原会签人 redo）需重启后端生效——今天重启后端（`env -u SERVER__PORT mvn spring-boot:run`，PID 70789，监听 9000）。

## 彻底移除会签模式（代码 + 数据库）

皇上评估后会签实现复杂、真有需求可新建流程做收集，决定移除会签模式。

- 数据库：`DROP` `flow_template_node.approve_mode`、`flow_task_dispatch_node.approve_mode` 两列；`flow_schema.sql` 删对应两行。
- 后端：
  - 实体 `FlowTemplateNode`/`FlowTaskDispatchNode` 删 `approveMode`；`TaskDetailVO` 删 `todoApproveMode/todoPendingCount`；`SubmitResultVO` 删 `waitingMeeting`。
  - `FlowTaskService`：`submit` 删 approveMode 判断、meetingWait、会签等待返回、waitingMeeting；保留「任一处理人提交即消解同节点其他待办」为固定行为；`resolveNextNode` 会签特判还原；`getDetail` 会签字段填充删；reject 的 `targetIsMeeting` 特判还原；回填 `isMeetingNodeOf` 特判删、`latestNodeWithFormData` 还原无处理人参数。
  - `FlowDispatchService` 快照复制删 setApproveMode。
- 前端：
  - `formdesigner`：删审批模式区块、「会签」节点标签（tag-meeting）、`onApproveModeChange`、`approveMode` 新建/复制字段、fetchDetail 归一；条件分支还原「非结束节点可配置」。
  - `ProcessDetail`/`ConfirmActionModal`：删 isMeetingWait/meetingWait/等待文案/nextHandlerIds 会签条件，提示还原。
  - `FlowChain`：删 meeting/formSubmissions/逐行一键展开（还原为单份 latestDone 展示 + 单人实际处理人）；删 expandedSubIds/toggleSub/isSubOpen/setAllSubs/sub-* 样式。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端重启监听 9000（PID 79431）。
- 说明：去掉会签后多处理人节点行为 = 「任一完成即流转、其余待办自动结束」。

## 消息通知闭环（预留通道）

皇上要求补齐「流转/退回/催办/下发」处理人无感知问题，但只预留接口、真推送由内网接管。

- 现状勘察：系统已有占位 `RobotService.sendRobot(receiverYyytIds, title, messageType, content)`（仅落日志，内网接管真推送）；催办只写 flow_task_log、未接任何通道；无站内信表。
- 新增 `FlowNotifyService`（flowtask.service）：封装 `notifyDispatch/notifyFlowNext/notifyRejectRedo/notifyUrge`，统一过滤接收人 + try-catch，通知失败绝不影响业务。
- 接入点：
  - 下发：`FlowDispatchService.generatePeriod` 新 9 参重载加 `notifyMembers`（8 参委托默认 true），循环后通知 memberUids；controller `POST /flow-dispatch/{taskId}/periods` 解析 body `notify`（默认 true）。
  - 补人：`addMembersToDispatch` count>0 后通知实际新增者。
  - 通过流转：`FlowTaskService.submit` 创建下一节点待办后通知 nextHandlerIds。
  - 退回：`FlowTaskService.submit` 退回分支通知 origHandlers（去发起者本人）。
  - 催办：`FlowTaskService.urgeTask` 通知 pending 节点处理人；`JobService.urgeScan` 每日自动催办同。
- 前端：`GeneratePeriodModal` 人员卡底部加「下发后通知各处理人」checkbox（默认勾选，payload.notify）；`EditTask`/`ConfigTemplateEdit` 催办文案由「仅写日志不真实通知」改为「自动催办/通知处理人」。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端重启监听 9000（PID 81925）。

## 任务关联「汇总←收集」功能（阶段一：处理端闭环）

皇上拍板：管理员全局可关联 + 处理人对自己收到的成员任务可发起收集；收集=完整任务配置(单次)+期次；配置级+成员级结合；只做展示+进度聚合不自动回填。

- 数据库：新表 `flow_task_link`（实库 + schema.sql 表18；主键 varchar(32)，与实库一致——注意 schema.sql 旧表仍 bigint 主键、与实库脱节）。
- 后端：
  - 实体 `FlowTaskLink`、`FlowTaskLinkMapper`。
  - `FlowTaskLinkService`：create（同源同目标幂等校验）/ listBySource(taskId|dispatchId) / listByTarget / remove（仅创建人或超管）；`toVO` 聚合目标配置名+期次(未落期次取最新一期)+收集人数/已收份数（成员任务 status=已结束 计已交）。
  - `TaskLinkVO`；`FlowTaskLinkController` /flow-task-link {POST、GET by-source/by-target、DELETE /{id}}。
  - 「发起收集」复合：`FlowDispatchService.launchCollect(sourceTaskId, templateId, taskName, memberIds, endTime, notifyMembers)`——不要求部门管理员（绕 checkDeptAdmin，仅校验发起人是该成员任务当前处理人/待办人/创建人），建收集配置(cycle=单次/启用/归属发起人 deptIdOf 可能为 null)→ saveMembers → generatePeriod(manual) → 写 flow_task_link(source_task_id/source_period_id/source_dispatch_id)。controller POST /flow-dispatch/collect-launch。
- 前端：
  - `FlowDispatchService.js` 加 5 API（create/getTaskLinksBySource/getTaskLinksByTarget/removeTaskLink/launchCollect）。
  - `CollectLaunchModal.vue`（taskprocess/components）：选启用模板/命名(默认 源任务名-材料收集)/UserPicker 选收集对象/截止 datetime 默认+7天/通知开关。
  - `ProcessDetail.vue`：右栏顶部「材料收集」区块（发起按钮/卡片列表：任务名、期次截止、已收 x/n 进度条、状态、查看跳期次人员页、解除）；处理中可发起、只读可看。fetchDetail 后 loadCollectLinks。
- 待续（阶段二）：任务管理(flowdispatch)管理员手动关联入口与展示（配置级）；收集任务侧（期次人员/成员任务）「服务于汇总任务」反链；卡片跳转的可见性确认。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端重启监听 9000（PID 82880）。

### 重构：关联改为「选择已有任务/期次」，不做处理端一键建收集

皇上调整：材料收集不要在处理流程链时新建，而要用户(在任务管理或处理端)从「已有任务/期次」里选来关联；任务管理每行加「关联」；期次和任务都可作为关联对象；被关联方也要展示；一键建收集(launchCollect)没用，删。

- 删除：`FlowDispatchService.launchCollect`（含其 FlowTaskLinkMapper 注入与 import、controller /collect-launch）；前端 `CollectLaunchModal.vue` 删除、`FlowDispatchService.js` 的 `launchCollect` api 删除。
- 增强：`TaskLinkVO` 加 source 侧名称(sourceDispatchName/sourceTaskName/sourcePeriodName)，`FlowTaskLinkService.toVO` 填充（供被关联方反链展示）；`create` 校验目标任务存在、所选期次必须属于所选任务。
- 前端新组件 `@/components/RelateTaskModal.vue`（通用）：选择「已有任务(getDispatchTaskList)」+ 可选「该任务下某期次(getTaskList 期次分页)」→ createTaskLink；处理端与任务管理共用。
  - 任务管理 `index.vue`：行内操作加「关联」按钮打开 RelateTaskModal(source=该配置)；展开面板顶部新增「任务关联」区块，展开时并行加载 by-source(本任务关联的收集任务+进度) 与 by-target(被其他任务/成员任务关联) 并可解除。
  - 处理端 `ProcessDetail`：入口按钮改「关联收集任务」→ RelateTaskModal(source=当前成员任务+所属期次)，卡片展示保留（by-source）。
- 关联粒度结论（问答环节）：主模型「任务配置↔任务配置」，期次可选精确；不把期次作为关联主语。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端重启监听 9000（PID 84014）。

### 关联目标扩展：可选择「我收到的任务」（成员任务）

皇上指出关联选择下拉只出"任务配置"不对，也应能选到自己收到的任务（成员任务实例）。

- 库表：`flow_task_link` 加列 `target_task_id`（实库 ALTER + schema 表18 DDL 同步）。
- 后端：`FlowTaskLink` 实体加 `targetTaskId`；`TaskLinkVO` 加 `targetTaskId/targetTaskName`；`FlowTaskLinkService.create` 支持目标=成员任务（反查成员任务并冗余补全所属任务配置 target_dispatch_id 与期次 target_period_id，列 target_dispatch_id 非空约束满足；去重按 target_task_id）；`toVO` 目标为成员任务时展示单条状态进度（member=1，status=已结束→done=1）。
- 前端：`RelateTaskModal` 目标切换两个来源：①任务配置（可选期次，getDispatchTaskList/getTaskList）②我收到的任务（getMyTodoList POST /flow-task/my-todo，按 taskId 去重，选中成员任务传 targetTaskId）。展示卡名称优先 targetTaskName。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端重启监听 9000（PID 84568）。

### 收敛：只保留「创建的任务 → 我收到的任务」，砍配置↔配置

皇上拍板：任务配置↔任务配置实用性差，去掉；实用场景 =「创建的任务去关联收到的任务」。

- `FlowTaskLinkService.create` 收紧：目标必须是成员任务（`targetTaskId` 必填，不再接受纯配置/期次级 target），反查成员任务冗余补全所属配置与期次；重复按 source + target_task_id 判断。`toVO` 对历史纯配置 target 数据仍保留展示兜底。
- `RelateTaskModal` 精简：去掉"任务配置/期次"目标 tab，只保留「我收到的任务」选择（getMyTodoList，按 taskId 去重），提交仅 targetTaskId。
- 展示语义改"任务关联"：ProcessDetail 区块标题改「任务关联」，卡片状态 member==1 显示 已完成/进行中，点击跳到成员任务详情(/task-process/detail)；管理端区块同收敛，名称可点击跳成员任务，被关联行标注"→ 目标成员任务名"。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端重启监听 9000（PID 84935）。

### 关联增强：可填关联说明 + 点击卡片查看完整任务

皇上要求：① 关联时填关联说明；② 点击关联任务能看到该任务所有情况。

- 前端 `RelateTaskModal`：加「关联说明」textarea（选填，maxlength 120），提交带 `remark` 字段（后端 create 直接 @RequestBody FlowTaskLink 已透传落库，toVO 已返回 remark，无需后端改动）。
- `ProcessDetail` 任务关联卡片：整卡可点击（@click goCollectPeriod，跳成员任务详情 /task-process/detail 看完整流程链/表单/历史），解除按钮 @click.stop；卡片显示 `lk.remark` 关联说明行（cc-remark 样式）。
- `flowdispatch/index.vue` 关联区块：outLinks 行整行可点跳转（link-row-go + title 提示），解除按钮 .stop；outLinks 与 inLinks 均展示 remark 说明行（link-remark-row，flex-basis 100% 换行）。
- 验证：前端 eslint 0 error（纯前端改动，热更新即生效）。

### 关联模型收敛为「期次 → 我收到的任务」

皇上拍板（先问答后开工）：任务级（配置↔收到的任务）关联仍不对——后续期次下发、人员变动会让配置级关系语义漂移。收敛为最具体形态：**source = 我创建的某任务的某一期次（source_period_id 必填），target = 我收到的某条具体成员任务（target_task_id 必填）**。存量数据全部清空重来。

- 存量：`TRUNCATE flow_task_link`（实库已清 0 条）。
- 后端 `FlowTaskLinkService.create`：source 必须是期次且属于当前登录人创建的任务（非超管校验 cfg.creatorId==当前人），自动冗余 source_dispatch_id=期次所属任务配置、source_task_id 置 null；target 校验存在并冗余补全所属配置/期次；幂等按 source_period_id+target_task_id。`listBySource` 增加 periodId 参数（dispatchId 仍可按配置查全部期次关联）；`listByTarget` 增加 targetTaskId 参数（收到的任务侧反查）。Controller by-source/by-target 同步加 periodId/taskId 参数。
- 前端：
  - `FlowDispatchService.js`：getTaskLinksBySource/getTaskLinksByTarget 均支持 (taskId, dispatchId, periodId)。
  - `RelateTaskModal`：改为"来源期次"形态（props 改 sourceDispatchId/Name + sourcePeriodId/Name），提交仅 sourcePeriodId+targetTaskId+remark；移除 sourceTaskId。
  - `flowdispatch/index.vue`：删任务行「关联」按钮与展开顶部「任务关联」区块；**期次列表每期操作列加「关联」**按钮（openRelate(t,p)）；期次行下用 `<template v-for>` 增加第二行展示本期次关联（pl-* 样式：目标任务名/完成状态/关联说明/解除，不跳转）；loadTaskDetail 只加载 outLinks（按 source_dispatch_id 查全部期次），按 sourcePeriodId 分组到 periodLinksOf；删 inLinks/goRelatedTarget 及旧 link-* 样式。
  - `ProcessDetail`：区块改「关联我的任务」反链展示（getTaskLinksByTarget(taskId=当前收到的任务)，卡片显示 来源任务/期次 + 关联说明 + 关联时间，不提供解除/跳转）；删除 RelateTaskModal 挂载/import、发起按钮、collectLinks/loadCollectLinks/goCollectPeriod 等旧方法。
- 验证：后端 mvn compile 通过、前端 eslint 0 error（剩余 2 处 v-show 属性顺序 warning 为既有）、后端已重启（见下）。

### 期次关联展示收进弹窗（不在行内直铺）

皇上反馈：期次行下直接展示关联太占地方，改为收到「关联」弹窗里：折叠面板/弹窗式展示详情。

- `flowdispatch/index.vue`：移除期次行下的关联第二行（period-link-tr/pl-* 整块与样式、periodLinksOf/linkDone/removeRelLink 方法、loadTaskDetail 的 linkRes/outLinks 加载、taskData.outLinks 字段、import getTaskLinksBySource/removeTaskLink）；表格恢复单行；「关联」按钮保留在期次操作列。
- `RelateTaskModal` 升级为「期次任务关联管理」：打开即加载该来源期次的已有列表（getTaskLinksBySource(null,null,periodId)，展示目标任务名/完成状态/关联说明/关联时间，可逐条解除 removeTaskLink），下方为新增表单（选我收到的任务时自动排除已关联项）；新增/解除成功后仅刷新弹窗内列表，父级不重载任务。
- 后端无改动；前端 eslint 0 error 0 warning（热更新生效）。

### 关联弹窗改版：宽表格 + 折叠展开 + 新增独立二级弹窗

皇上反馈：关联弹窗做宽一点；已关联不要卡片列表，改「任务列表式表格 + 折叠面板」；新增收成按钮、点开二级弹窗。

- `RelateTaskModal` 重做：外层 dialog 940px。「期次任务关联」主区 = el-table(border) 展示已有关联：expand 列(行首箭头点击展开详情：关联说明/目标任务(含所属期次)/关联时间/解除按钮) + 目标任务列 + 完成状态列 + 有无说明列 + 关联时间列 + 操作列(解除)；空态给「新增关联」按钮。顶部工具条右侧「新增关联」按钮打开**二级 el-dialog**（选我收到的任务(已关联自动排除) + 关联说明 + 建立关联），成功后刷新主表格。
- 无后端改动；前端 eslint 0 error。

### 关联弹窗最终版：仿任务管理「期次列表」风格自绘表格

皇上反馈 el-table 版太丑，要求参考任务管理展开区的期次列表表格外观，仅去掉「期次名称」列。

- `RelateTaskModal` 主表改**自绘 table**（对齐 index.vue `.period-table`：表头浅蓝底 font-bold、hover 行高亮、.action-link 文字操作、sub-text），dialog 宽 860px。列：目标任务（font-bold + 所属期次 sub-text）/ 完成状态 tag(进行中=主题蓝/已完成=绿) / 关联说明（超长省略+title）/ 关联时间 / 操作(解除)。
- 行点击折叠展开：行首箭头旋转 + 展开行高亮，第二行 detail 展示目标任务(含所属期次)/完整关联说明/关联时间/解除按钮。
- 「新增关联」按钮与二级新增弹窗保留；空态居中同款。
- 前端 eslint 0 error 0 warning（热更新生效）。

### 关联表格对齐「任务处理-期次列表」列结构（后端补目标任务数据）

皇上指明参照物即 `taskprocess/index.vue` 展开区期次列表（列：任务名称/起止时间/状态/流程进度），要求关联表格按此列展示目标任务行。

- 后端 `TaskLinkVO` 增 `targetStartTime`/`targetChain`（List<TodoNodeVO>）；`FlowTaskLinkService.toVO` 目标成员任务分支用成员任务自身 `startTime/endTime` 作起止、新增 `buildTargetChain`（复用 FlowTaskService 模板链+任务流转记录组装节点链、status 0/1/2 判定，需 FlowTaskNode/FlowTemplateNode 注入）。
- 前端 `RelateTaskModal` 主表改列：任务名称(省略号+tip+所属期次 tag) / 起止时间 / 状态(status-badge 进行中=蓝已完成=绿) / 流程进度(node-chain 编号节点 chip：已完成绿/进行中蓝实心/未开始灰) / 操作(解除)；行点击折叠展示详情（关联说明/关联时间/解除）。样式复制 taskprocess index 对应类。
- 验证：后端 mvn compile 通过、前端 eslint 0 error、后端已重启（PID 待确认）。

### 关联折叠面板对齐任务管理样式（弹窗加宽 + 展开显示流程链）

皇上反馈：弹窗再宽点；折叠展开样式要像任务管理（flowdispatch/index.vue 的 .task-panel 折叠面板：头行图标+名称+状态+箭头，点击展开 body）；展开后展示流程链。

- `RelateTaskModal` 改造成：dialog 1100px；已关联用 `.task-collapse/.task-panel` 折叠面板（复制任务管理同名样式到本地 scoped），每条关联 = 面板：
  - 头行：ct-icon 图标 / 任务名(+所属期次 tag + 完成状态 ct-status) / 说明摘要 ct-meta / 箭头(展开旋转)
  - 展开 body（.task-detail 浅底）：overview-grid 概览(所属期次/起止时间/完成状态) + 流程链 section（node-chain chips：已完成绿/进行中蓝/未开始灰）+ 关联说明(若有) + 底部(关联时间/解除按钮)
- 纯前端；eslint 0 error。

### 关联弹窗最终版2：表格=任务处理期次列表，展开行内嵌 FlowChain 流程链

皇上澄清：表格参考 taskprocess/index.vue 期次列表（列 任务名称/起止时间/状态/流程进度），展开时用 FlowChain 组件（横向/竖向流程链节点）。

- `RelateTaskModal`：dialog 1100px，主表 = 任务处理期次列表同款 table（列同上，行内 node-chain chip 概览，状态 badge，操作列 查看流程/解除）；点击行/「查看流程」展开第二行，异步 `getTaskDetail(targetTaskId)` 取完整详情后渲染 `<FlowChain :task-detail>`（自带横竖切换，title=任务名·流程链）；加载中骨架、失败回退 chips；展开区内 rel-extra 显示关联说明/关联时间/解除。import FlowChain + getTaskDetail。
- 纯前端；eslint 0 error 0 warning。

### 关联弹窗微调：去起止列 + 展开区任务名表头 + 加宽 1200

- `RelateTaskModal` dialog 1100→1200px；主表删除「起止时间」列（保留 任务名称/状态/流程进度/操作）；展开区 FlowChain 上方新增 `.rel-task-hd` 任务名称表头条（浅蓝底 + 左侧主题色竖条 + 任务名大标题 + 期次 tag + 状态 badge），FlowChain title 固定「流程链」。
- 纯前端；eslint 0 error 0 warning。

